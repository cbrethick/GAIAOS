# ============================================================
#  GaiaOS — API Configuration
#  ALL APIs below are 100% FREE — no credit card, no billing
#  Only NASA_FIRMS_KEY needs registration (2 min, free)
# ============================================================

# ── NASA FIRMS — Real wildfire/fire detections from satellite ─
# GET YOUR FREE KEY (2 minutes, no card):
#   1. Go to: https://firms.modaps.eosdis.nasa.gov/api/
#   2. Click "Get MAP_KEY"
#   3. Sign in / register free NASA Earthdata account
#   4. Key appears on screen — copy and paste below
NASA_FIRMS_KEY=f4c75ce587ced56a7fe9c6e2dbf863f7

# ── Everything below works with ZERO registration ────────────

# Open-Meteo — live weather for India (no key, always free)
OPEN_METEO_BASE=https://api.open-meteo.com/v1

# Open-Meteo Historical — ERA5 reanalysis 1940-present (no key)
OPEN_METEO_HIST=https://archive-api.open-meteo.com/v1

# Open-Meteo Flood API — river discharge India (no key)
OPEN_METEO_FLOOD=https://flood-api.open-meteo.com/v1

# Open-Meteo Air Quality — PM2.5, PM10 India (no key)
OPEN_METEO_AQ=https://air-quality-api.open-meteo.com/v1

# Nominatim OSM — search any city/village in India (no key)
NOMINATIM_BASE=https://nominatim.openstreetmap.org

# Overpass OSM — real roads/buildings data India (no key)
OVERPASS_BASE=https://overpass-api.de/api/interpreter

# NASA POWER — solar radiation + climate India (no key)
NASA_POWER_BASE=https://power.larc.nasa.gov/api/temporal/daily/point
